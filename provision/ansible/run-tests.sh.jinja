#!/bin/bash

# You should run this within screen

LOG_FILE=/tmp/test-log
> $LOG_FILE

echo 1 > /tmp/tests-running
EXTERNALS_HOST={{externals_host}} ./gradlew --no-daemon test --stacktrace >> $LOG_FILE
echo 0 > /tmp/tests-running
